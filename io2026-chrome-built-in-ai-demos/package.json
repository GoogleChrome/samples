{
  "name": "io2026-chrome-built-in-ai-demos",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "npx http-server",
    "build": "node build.js",
    "deploy": "npm run build && git add screenshots/ overrides/screenshots/ overrides/overrides.json demos.json index.html && (git diff --cached --quiet && echo 'Nothing to deploy.' || (git commit -m 'Update demos' && git push))"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.0",
    "dotenv": "^16.4.0",
    "http-server": "^14.1.1",
    "puppeteer": "^23.0.0"
  }
}
